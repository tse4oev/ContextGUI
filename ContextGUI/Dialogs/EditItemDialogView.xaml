<UserControl x:Class="ContextGUI.Dialogs.EditItemDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Grid>
        <StackPanel>
            <TextBlock Text="Название" FontWeight="SemiBold" Margin="0,0,0,6" />
            <TextBox Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,12"
                     IsEnabled="{Binding IsEditable}" />

            <TextBlock Text="Иконка (путь)" FontWeight="SemiBold" Margin="0,0,0,6" />
            <TextBox Text="{Binding IconPath, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,12"
                     IsEnabled="{Binding IsEditable}" />

            <TextBlock Text="Команда" FontWeight="SemiBold" Margin="0,0,0,6" />
            <TextBox Text="{Binding Command, UpdateSourceTrigger=PropertyChanged}"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     Height="80"
                     Margin="0,0,0,12"
                     IsEnabled="{Binding IsEditable}" />

            <TextBlock Text="Legacy-элементы не поддерживают редактирование."
                       Foreground="#A7A9B2"
                       Margin="0,0,0,6"
                       Visibility="{Binding IsLegacyHandler, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <TextBlock Text="Современные обработчики не поддерживают редактирование."
                       Foreground="#A7A9B2"
                       Margin="0,0,0,6"
                       Visibility="{Binding IsModernHandler, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#FF7A90" />
        </StackPanel>
    </Grid>
</UserControl>
